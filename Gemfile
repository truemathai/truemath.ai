source "https://rubygems.org"

# Mirrors the exact Jekyll + plugin versions GitHub Pages runs in production,
# so a local `bundle exec jekyll serve` matches the deployed site.
gem "github-pages", group: :jekyll_plugins

# Lock down to a compatible platform set.
gem "webrick", "~> 1.8"
